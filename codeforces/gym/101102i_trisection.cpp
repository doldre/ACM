#include <cstdio>
#include <cstring>
#include <cstdlib>
using namespace std;

const int MAXN = 2e5 + 5;
int x[MAXN], y[MAXN], cntx, cnty;
char str[MAXN];
int r, c;
int main() {
    int T; scanf("%d", &T);
    while(T--) {
        cntx = cnty = 0;
        scanf("%d%d%s", &r, &c, str);
        int len = strlen(str);
        for (int i = 0; i < len; i++) {

    }
}
